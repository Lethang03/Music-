project_id = "ywfwsklpmoogvfscjrja"

[functions.import-job]
# The function validates the supplied bearer token with Auth so it can return
# consistent JSON/CORS errors instead of gateway HTML responses.
verify_jwt = false

[functions.process-import]
enabled = true
verify_jwt = false
import_map = "./functions/process-import/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/process-import/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/process-import/*.html" ]
